import { Text } from '@src/components';
import React from 'react';
import { View } from 'react-native';

const WorkingTasks: React.FC = ({}) => {
  return (
    <View>
      <Text>Working Tasks</Text>
    </View>
  );
};

export default WorkingTasks;
